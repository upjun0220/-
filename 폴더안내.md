# 공모전 폴더 안내 (2026-07-31 정리)

정리 전에는 루트에 파일 60개가 섞여 있었습니다. 지금은 **루트에 README.md 와 이 파일뿐**입니다.

---

## 어디에 뭐가 있나

| 폴더 | 내용 | 언제 여나 |
|---|---|---|
| **01_현행코드** | 지금 돌아가는 코드 전부 (젯슨 + 노트북) | **일상 작업** |
| **02_레이더_원본코드** | `radar_live_full.py` 등 분리 이전 원본 | 판정 로직 확인·대조할 때 |
| **03_데이터** | 실측 jsonl 전부 (이벤트/판정로그/빈방) | 재학습·분석할 때 |
| **04_문서** | 설계 명세 · 진행보고 | 보고서 쓸 때 |
| **05_발표자료** | pptx · 발표용 이미지 | 발표 준비 |
| **06_부스_하우징** | 3D 프린팅 하우징 + 알루미늄 프레임 | 부스 제작 |
| **07_중간보고서_7.14** | 제출 완료된 중간보고서 원본 | 후속 보고서 참고 |
| **08_주차별_진행도** | 주차별 기록 (노션+코딩) | 회고 |
| **09_프로젝트_정보 / 09_참고자료** | 공모전 요강 · 참고 논문 | |
| **10_팀협업** | 팀원 코드 (승원이 파이프라인 등) | |
| **_구버전보관** | 죽었지만 근거로 남긴 코드 | 거의 안 열어도 됨 |
| **_삭제후보** | 지워도 되는 것 모아둠 → `삭제후보_목록.md` 확인 후 폴더째 삭제 | **한 번 확인 필요** |

---

## 01_현행코드 — 이것만 알면 됩니다

```
radar_common.py      양쪽 공용 상수 (CEILING_H·포트·이벤트 라벨). 여기만 고치면 양쪽 반영
jetson_sender.py     [젯슨] 판정 + 차단 + UDP 송신.  radar_common.py 와 함께 젯슨에 배포
console_ui.py        [노트북] PyQtGraph 관제 UI
sim_jetson.py        [노트북] 젯슨 없이 UI 검증하는 모의 송신기
verify_port.py       판정 이식 무손실 회귀 검증 (7,788건 대조)
작업결과_0731.md      분리 작업 상세 + 아침에 확인할 것

⚠ [8/03] _구버전_laptop_viewer.py (matplotlib 뷰어) 는 _구버전보관/ 으로 옮겼다.
   현행 폴더에 '_구버전' 파일이 섞여 있으면 파일명만 보고 잘못 여는 사고가 난다.
```

### 실행

```powershell
cd "$env:USERPROFILE\OneDrive\문서\Claude\Projects\공모전\01_현행코드"

# ★ 권장 — 진짜 jetson_sender.py 를 노트북에서 구동 (레이더·젯슨·torch 불필요)
python jetson_sender.py --simulate --fast   # 창 1
python console_ui.py --live 127.0.0.1       # 창 2
#   실제 판정 코드가 전부 돕니다. --fast 는 한 사이클 24초.

# UI 만 빠르게 보고 싶을 때 (프로토콜만 흉내내는 경량 시뮬레이터)
python sim_jetson.py                        # 창 1
python console_ui.py --live 127.0.0.1       # 창 2

# 판정 이식 검증 (어디서 실행해도 경로를 스스로 찾음)
python 01_현행코드\verify_port.py

# 일요일 젯슨 연결
python console_ui.py --live 192.168.0.50   # 젯슨 IP
```

---

## ⚠ 옮기면 안 되는 것

1. **`02_레이더_원본코드/radar_live_full.py`**
   `verify_port.py` 가 이 파일을 **판정 로직의 기준 원본**으로 대조합니다.
   지우거나 옮기면 "이식이 무손실이다"를 더 이상 증명할 수 없습니다.

2. **`03_데이터/이벤트_학습용/*.jsonl`**
   `verify_port.py` 와 `train_fall_classifier.py` 가 이 경로를 읽습니다.
   그리고 이 데이터는 **현장에서 몸으로 수집한 것이라 재생성이 불가능**합니다.

3. **`01_현행코드/radar_common.py`**
   `jetson_sender.py` · `console_ui.py` · `sim_jetson.py` 가 모두 import 합니다.
   젯슨에 배포할 때 **이 파일을 같이 올려야** 합니다.

폴더 이동으로 깨진 상대경로 2곳은 이미 고쳤습니다 (`train_fall_classifier.py`, `verify_port.py`).
이동 후 회귀 검증을 재실행해 **7,788건 불일치 0건**을 확인했습니다.

---

## 손으로 지워주셔야 하는 빈 폴더 2개

OneDrive 권한 때문에 제가 지울 수 없었습니다. 안은 비어 있습니다.

- `ui 및 코드 분리(중간보고서, 부스 제작 이후)/`  → 내용은 `01_현행코드/` 로 옮겼습니다
- `06_부스_하우징/_v5_openframe_보류/`  → 내용은 `06_부스_하우징/_구버전/v5_오픈프레임/` 으로 옮겼습니다
