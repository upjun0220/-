# BACKLOG — 언젠가 처리할 제품 작업·개선 이슈

> **여기는 쌓여도 된다.** `HANDOFF.md` 와 반대다.
> **Claude Code 는 이 파일을 읽지 않는다.** 여기서 골라 HANDOFF 의 Next actions(최대 3)로 올라간 것만 지시가 된다.
> 올릴 때 여기서 지운다. 두 곳에 같은 항목이 있으면 안 된다.
> 질문·판단 요청은 여기가 아니라 `AI_BRIDGE/OUTBOX.md` 다. 설계 근거가 확정되면 `04_문서/` 로 옮긴다.

기준일: 2026-08-03

## A. 근거 확실 — 올릴 준비 됨

| 항목 | 위치 · 근거 |
|---|---|
| 평면도에 개발자 메모 노출 | "데모 레이아웃 · 실사용 시 facility.py 를 시설 도면으로 교체" 가 항상 표시. 심사 영상에 찍힌다 |
| 드로어 "검색 중" 중복 | `SopView` 의 `stat` 과 `src` 가 같은 상태를 각각 표시 (`replay_still.png` 확인) |
| 하드코딩 RGB 2곳 | `console_ui.py:505` `mkBrush(34,211,238,150)` / `radar_core.py:847` `mkBrush(0,204,255,160)` — 후자 `#00CCFF` 는 팔레트에 없는 색 |
| 타임라인 영문 원문 | `ALERT Zone A: stationary_anomaly [warning]` — 한글 UI 에 영문 로그 |
| "0초째 지속되고 있습니다" | 경보 발생 순간이라 경과 0초. 문구 문제 |
| README 8/1 기록 정정 `[CW]` | "실데이터 재생 57항목 0건" 은 잘못된 수치 |

## B. 확인 후 판단

| 항목 | 확인된 것 |
|---|---|
| 폰트 6단계 비율 불규칙 | 1.368 / 1.267 / 1.364 / **1.100 / 1.111**. 하위 3단계 사실상 구분 불가. `F_BODY` 14.7px < 권장 16px. **목표 스케일 미정** — 잘림 재검증 필요 |
| 재생 디버그 문자열 | `(재생 label=still, conf=80%)` 가 타임라인에 표시. 젯슨 실물에서도 나오는지 **미확인** |

## C. 시각 개선 — 규칙이 아니라 개별 승인 사항

- 사이드바 아이콘이 유니코드 문자(`▦ ◉ ▤ ⌂ ⊞ ⚙`) — `⌂` `⊞` 만 작고 위로 뜬다. 폰트 없는 PC 에선 두부(□)
- 평상시 우측 열 하단 200px+ 공백
- 경보 잠금 문구가 좌하단 사이드바 — 가장 안 보는 자리
- 드로어 [확인했습니다] 가 CYAN — 경보 맥락과 불일치
- 계측 4카드를 수평 telemetry bar 로 (card soup)
- 계측값에 정상 범위·임계값 병기 — "이상도 1.6배" 가 위험한지 지금은 알 수 없다
- 설정 창 제목 중복(타이틀바 + 본문 헤더) · 폼 필드 폭 과다
- 평면도 우측 무명 사각형 4개

## D. 시점 조건이 붙은 것

| 항목 | 조건 |
|---|---|
| 애니메이션 도입 | 10 Hz 부하 실측 후 |

> 라이트 모드 구현과 `states → dynamic property + QSS` 전환은 **`04_문서/라이트모드_팔레트_확정_0803.md` 가 정본**이다. 여기에 중복해 적지 않는다.
