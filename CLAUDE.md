# CLAUDE.md — Radar-Guard 프로젝트 규칙

> 2026 한이음 드림업 공모전 · 비접촉 레이더 기반 고보안 현장 작업자 안전 관리 에이전트
> 담당: 홍유빈 — 시스템 통합 / UI / RAG
> 이 파일은 Claude Code와 Cowork 양쪽이 세션마다 읽는다. **추측하지 말고 여기 적힌 것을 따른다.**

---

## 0. 가장 중요한 3가지

1. **이건 안전 시스템이다.** 낙상·감전을 놓치면 사람이 다친다. "아마 될 것"으로 넘어가는 변경은 없다. 모든 수정은 §5의 검증을 통과해야 한다.
2. **판정 경로와 표시 경로는 절대 섞지 않는다.** 젯슨이 판정하고, 노트북은 그것을 그린다. UI 계산 결과를 판정에 되먹이면 안 된다.
3. **모르면 묻는다.** 애매한 지점에서 임의로 정하지 말고 멈추고 물어본다. 제안을 받아도 타당성을 먼저 검토하고, 걸리는 게 있으면 그대로 진행하지 말고 대안을 제시한다.

---

## 1. 아키텍처 (2026-07-28 최종 확정)

```
[IWR6843 레이더] --UART--> [Jetson Orin Nano]  --UDP :5005--> [노트북]
                            판정 전용                            관제 UI + LLM + SOP DB
                            classify() / LSTM-AE              <--UDP :5006-- 제어 명령
                            차단기 제어
```

| | 젯슨 (엣지) | 노트북 (관제) |
|---|---|---|
| 역할 | 낙상·감전 **판정**, 차단기 차단 | 화면, LLM 브리핑, SOP RAG |
| 파일 | `jetson_sender.py` | `console_ui.py` + `radar_core.py` |
| 환경 | JetPack 7.2 (R39 / CUDA 13.2), sm_87 | Windows, MX450 (VRAM 2GB) |
| 접속 | 시리얼 **COM13** | — |
| LLM | 없음 | Ollama, CPU 추론 **13.45 tok/s** 실측 |

**젯슨이 죽어도 판정은 젯슨에서 끝난다.** 노트북 UDP가 끊겨도 차단은 동작해야 한다. 노트북의 제어 명령(`CTRL_PORT`)은 젯슨 로컬 제어의 **대체가 아니라 보조**다.

---

## 2. 폴더 구조

| 경로 | 내용 | 누가 만지나 |
|---|---|---|
| `01_현행코드/` | **현행 코드. 여기만 실행된다.** | Claude Code |
| `02_레이더_원본코드/` | 팀 원본. `radar_live_full.py`가 판정의 정본 | 읽기만 |
| `03_데이터/` | 실측 jsonl (70MB) | 읽기만 |
| `04_문서/` | 설계·진행보고 | Cowork |
| `05_발표자료/`, `06_부스_하우징/`, `07_중간보고서_7.14/` | 산출물 | Cowork |
| `08_주차별_진행도/` | 학습 기록 (week1~5) | 읽기만 |
| `10_팀협업/` | 팀원 코드·인터페이스 명세 | 협의 후 |
| `_구버전보관/`, `_삭제후보/` | **참조 전용. 절대 여기 코드를 고치지 않는다.** | 금지 |
| `README.md` | 개발 일지 (2,653줄) | §6 형식 준수 |

**`_구버전보관/`에도 `console_ui.py`가 있다.** 파일명만 보고 열지 말고 항상 경로를 확인한다.

---

## 3. 현행 코드 (`01_현행코드/`)

### 실행 파일
| 파일 | 줄수 | 역할 |
|---|---:|---|
| `console_ui.py` | 2,953 | **화면 전부 + 진입점.** `build_app()` → `ConsoleV2` |
| `radar_core.py` | 1,953 | `RadarLink`(수신 스레드) · `PoseEstimator` · `SopEngine` · `Track3D` · `PreparePage` · 팝업 6종 |
| `radar_common.py` | 375 | **젯슨/노트북 공용 상수. 양쪽이 같은 파일을 본다.** |
| `facility.py` | 123 | 시설 평면도 좌표 |
| `jetson_sender.py` | 1,689 | 젯슨 측 — 판정 + UDP 송신 |

`console_ui.py`는 `radar_core.py`를 **import** 한다. 복사가 아니라 같은 코드 객체다. 이 관계를 깨지 않는다.

### 검증 · 도구
| 파일 | 용도 |
|---|---|
| `테스트_v1결함_재발검사.py` | v1에서 고친 결함 재발 검사 (기준: 0건) |
| `테스트_레이아웃_검증.py` | 4해상도 잘림/겹침 (기준: 0건) |
| `테스트_실데이터_재생.py` | 실측 jsonl → UDP → UI 전 경로 |
| `테스트_평면도_경보흐름.py` | 사전경보 → latch → 자동전환 → 종료 |
| `verify_port.py` | `classify()` 이식 무손실 회귀 (numpy만 필요) |
| `verify_jetson_safe.py` | 노트북용 수정이 젯슨을 안 깬다는 증명 |
| `sop_doctor.py` | 벡터DB 실제 내용 진단 |
| `replay_jsonl.py` | 실측 재생기 (눈으로 볼 때) |
| `sim_jetson.py` | 난수 모의 송신기 (프로토콜 검증용, numpy만) |

---

## 4. 절대 하지 말 것

- **레이더 `.cfg` 수정 금지.** 포인트 수 부족은 각분해능 28° 병목이 원인이고 cfg로 해결되지 않는다. 누적으로만 다룬다.
- **판정 상수를 UI 편의로 바꾸지 않는다.** `radar_common.py`의 임계값(`VIB_DS_THRESH`, `CURR_LIMIT`, `VOLT_MIN` 등)을 바꾸면 `verify_port.py`가 깨진다. 바꿔야 하면 근거와 재학습 계획을 먼저 낸다.
- **`radar_common.py`는 젯슨과 노트북이 공유한다.** 한쪽 편의로 고치면 다른 쪽이 죽는다.
- **`_구버전보관/`, `_삭제후보/` 안의 파일을 수정하지 않는다.**
- **`03_데이터/`의 jsonl을 덮어쓰지 않는다.** 재수집 비용이 크다.
- **RED(`#EF4444`)는 위험 경보 외에 절대 쓰지 않는다.** 색이 등급을 의미한다.
- **화면에 설명 문구를 늘리지 않는다.** §7 참조.
- **없는 수치를 그럴듯하게 채우지 않는다.** 데이터가 없으면 `—`로 둔다. 관제 화면의 거짓 수치는 결함이다.

---

## 5. 수정 후 반드시 실행 (환경: **내 PC PowerShell**)

`console_ui.py` / `radar_core.py` / `radar_common.py`를 건드렸으면:

```powershell
cd "C:\Users\82102\OneDrive\문서\Claude\Projects\공모전\01_현행코드"
python -m pyflakes console_ui.py radar_core.py radar_common.py facility.py
python 테스트_v1결함_재발검사.py
python 테스트_레이아웃_검증.py
python 테스트_실데이터_재생.py
python 테스트_평면도_경보흐름.py
```

`jetson_sender.py` / `radar_common.py`를 건드렸으면 추가로:

```powershell
python verify_port.py
python verify_jetson_safe.py
```

**통과 기준은 전부 0건 / 종료코드 0이다.** 하나라도 NG면 완료라고 보고하지 않는다.
검증 스크립트는 `QT_QPA_PLATFORM=offscreen`으로 돌아 창이 뜨지 않는다.

---

## 6. README 작성 형식

README는 개발 일지이자 **보고서 원본**이다. 그래서 형식이 정해져 있다.

- **문제 → 해결 순서.** "무엇을 만들었다"가 아니라 "무엇이 문제였고 왜 이렇게 풀었다".
- 날짜 헤더로 세션을 구분한다 (`# 🟢 8/01 — 제목`).
- 항목마다 **문제 / 해결**을 굵게 구분하고, 근거가 되는 실측 수치를 넣는다.
- 검증 결과는 숫자로 쓴다 ("v1 결함 재발 35항목 0건").
- 최신 세션이 위로 간다.

---

## 7. UI 설계 원칙 (2026-07-29 확정)

1. **3D 포인트 클라우드가 메인이다.** 다른 요소가 이걸 밀어내지 않는다.
2. **평소 화면과 경보 화면을 분리한다.** 경보 시 볼 것만 남는다.
3. **화면에 설명을 쓰지 않는다.** 관제 근무자는 읽지 않는다. 색과 위치로 말한다.
4. **버튼은 뒤로 뺀다.** 상시 노출은 상태 표시, 조작은 한 단계 뒤.
5. **위험 조작은 확인을 받는다.** 전원 재투입 등은 `confirm()` 필수.

배제 확정: FFT 스펙트럼, SYSTEM HEALTH 패널. (개발용 정보이지 관제 정보가 아니다)

LLM은 4레이어로 쓴다 — L1 브리핑 / L2 XAI 근거 / L3 RAG / L4 질의. **LLM은 안전 조치의 필수 경로가 아니다.** 실패해도 하드코딩 즉시조치와 검색 원문은 이미 화면에 있어야 한다.

---

## 8. 알려진 물리적 한계 (코드로 고치려 들지 말 것)

- **각분해능 28°** → 포인트 수가 근본적으로 적다. 8점/프레임을 10프레임 누적해 ~80점으로 쓴다.
- **정지한 사람은 FMCW 레이더가 놓친다.** 도플러가 0이라 사라진다. 그 자리를 다중경로·바닥반사 링이 채운다.
  → 낙상 경보 중인데 점군이 "서 있음"이면 **사람이 아니라 추적을 놓친 것**이다. 형상을 지우고 이유를 표시한다.
- **젯슨에 RTC 배터리가 없다.** 부팅마다 시계가 틀어진다. **모든 경과시간은 노트북 수신 시각 기준**으로 계산한다.
- **노트북 GPU(MX450)로 LLM 가속 불가.** VRAM 2GB + 드라이버 452. 7~8B 모델 상향은 폐기됐다. 병목은 속도가 아니라 한국어 품질이다.
- **임베딩은 `bge-m3`로 통일.** 적재와 검색이 다른 모델이면 차원이 안 맞아 조용히 0건이 나온다 (`sop_doctor.py` 참조).

---

## 9. 코딩 규칙

- **주석은 한국어.** 왜 이렇게 했는지를 쓴다. 특히 실측으로 알아낸 것은 날짜와 수치를 남긴다 (`⚠ [8/01 실측] …`).
- **파일 상단 docstring에 실행 방법을 적는다.** 어느 환경(PowerShell / 젯슨 / VS Code)인지 명시한다.
- **예외를 조용히 삼키지 않는다.** 실패는 화면이나 로그에 표시한다.
- **센티넬 값을 UI로 흘리지 않는다.** (`1e9` 대신 `None` → "연결 전")
- 새 기능은 **필요한 최소만** 쓴다. 추측 기반 추상화·미래 대비 스캐폴딩 금지. 단, **검증 스크립트와 안전 방어 코드는 이 규칙의 예외**다 — 중복이라도 남긴다.

---

## 10. Claude Code ↔ Cowork 역할 분리

같은 폴더를 둘이 동시에 열 수 있지만 **같은 파일을 동시에 고치면 서로 덮어쓴다.**

| | Claude Code (PowerShell) | Cowork (데스크톱 앱) |
|---|---|---|
| 담당 | `01_현행코드/` — 코드 작성·실행·디버깅 | `04_문서/`, `05_발표자료/`, `06_부스_하우징/`, README, Notion |
| 이유 | 실제로 `python`을 돌리고 COM13·젯슨에 붙을 수 있다 | 문서·발표·조사에 강하고 파일 공유가 편하다 |

**한쪽이 작업 중인 파일은 다른 쪽에서 열지 않는다.** 코드 세션이 끝나면 `git commit` 후 문서 작업으로 넘어간다.

---

## 11. 일정

- **2026-08-17** 멘토링
- **10~11월** 최종 발표

심사는 **영상 시연 우선**. 동상 이상 확정 시에만 현장 시연. → 외관보다 **레이더 성능**이 최우선이다.
