# HANDOFF

## State

- Updated: 2026-08-26 · Codex
- Branch: main
- Commit: 커밋 직전
- Working tree: 현행 코드와 인계 문서만 커밋하며 사용자 문서·분석 출력·백업 JSON은 제외한다.

## Current objective

협착 R2 shadow의 정상 동작 오탐을 실측해 실판정 승격 여부를 결정한다.

## Verified baseline

- pyflakes 필수 대상 경고 0건 · v1 결함 재발 54항목 실패 0건.
- 레이아웃 4해상도 잘림·겹침 0건 · 실데이터 재생 73항목 실패 0건 · 평면도 흐름 16항목 실패 0건.
- 판정 이식 9,580건 불일치 0건 · 젯슨 안전성 101항목 실패 0건.
- 젯슨 sender SHA-256 `5b00451d1604780a03de85a2816d3324eec35d486fed1172834f140c3458dd1c`이며 parser·sender·사용자 UI와 PC 관제 UI가 실행 중이다.

## Next actions

1. 승원·성준·민석·재국의 정상 서기·걷기·일반 웅크리기를 동일한 차단 후 게이트에서 각각 라벨링해 수집한다.
2. 기존 candidate·R2·q70/q80/q90 비율을 사람 단위로 비교해 재현율과 동작별 오탐률을 계산한다.
3. R2가 신규 사람 재현율 80% 이상·걷기 오탐률 10% 이하일 때만 사용자 승인 후 실판정 전환을 검토한다.

## Blockers / unknowns

- 8/26 4명 실측은 협착 양성만 있어 R2의 정상 동작 오탐률을 알 수 없다.
- 5회 동작별 시작·종료 마커가 없어 현재 shadow 로그만으로 20회 개별 검출률을 분리할 수 없다.

## Acceptance

- 사람별 양성·음성 구간에 독립 라벨이 있고, 기존·R2·개인 기준선의 표본 수와 재현율·오탐률이 숫자로 재현된다.
- 실판정 전환 전 `verify_port.py` 9,580건 불일치 0건과 `verify_jetson_safe.py` 실패 0건을 유지한다.
