# HANDOFF — Cowork ↔ Claude Code 인계 노트

> **이건 로그가 아니라 화이트보드다.** 항상 "지금"만 적는다. 끝난 항목은 지우고, 남길 가치가 있으면 `README.md`로 옮긴다.
> **변경 내역은 여기 쓰지 않는다.** 무엇이 바뀌었는지의 정본은 `git log` / `git diff`다. 여기엔 git이 담을 수 없는 것만 쓴다 — **의도 · 판단 · 막힌 지점.**
> 각 역할은 **자기 섹션만** 고친다. 남의 섹션은 읽기만 한다.
> 갱신 시점: **세션을 끝낼 때.** 작업 도중에 계속 쓰지 않는다 (동시 편집 충돌).
> 분량: 각 섹션 **10줄 이내.** 넘치면 오래된 것부터 지운다.

---

## 0. 현재 상태 (양쪽 다 읽는다 · 마지막에 작업한 쪽이 갱신)

- **최종 갱신**: 2026-08-03 · Claude Code
- **git**: `main` / 이번 세션 작업 커밋 완료(레이아웃·vib·set_mode·prewarm 침묵 수정 전부 포함)
- **지금 무슨 단계인가**: §1 1순위 레이아웃, §1 2순위 vib 도식 색, prewarm() 침묵 수정 **모두 해결·검증 완료**(근거 §2). §1 ① (`cd` 없이 돌게 하기) 착수 예정.
- **다음 최우선**: **8/04(월) 젯슨 통합시험**(§1 런북 참조). §2 에 판단 필요 항목 2개(링크 갭, on_alert 지연·8/04 실측 후 판단) 남겨둠 — 급하지 않으면 젯슨 연결 먼저.
- **[8/02 우선순위 변경 · Cowork]** 심사가 영상 시연 우선이라 잘린 글자가 영상에 그대로 찍힌다는 이유로 레이아웃을 vib보다 앞세웠음 — 이번 세션에 반영 완료.

---

## 1. Cowork → Claude Code  (설계 결정 · 지시)

> Cowork만 고친다. Claude Code는 읽고, 작업 시작 전에 여기 내용과 어긋나는 게 있으면 먼저 묻는다.

- **[8/02·1순위] 레이아웃 잘림 9건 — 원인 규명 완료. 아래대로 수정 승인.**
  - **확정된 원인**: pip 설치 PyQt5 의 offscreen 플러그인이 번들 `Qt5/lib/fonts` 를 못 찾아 `families()=0` → `resolve_font()` 가 한 번도 매칭 못 하고 108줄 초기값이 유지됨 → 합성 폰트 메트릭이 글자 폭을 부풀려 **허위 잘림**.
    근거 3: ① offscreen families 0 / 실제 창 349 ② `QT_QPA_FONTDIR` 지정 시 offscreen 도 0→251, 잘림 9→0 ③ 실제 창(Noto Sans KR)에서 잘림 0건, 스크린샷 육안 확인.
  - **`console_ui.py` 의 렌더링에는 결함이 없다.** 고칠 대상은 테스트 환경이다.

  - **승인 — 아래 4가지를 함께 반영할 것**
    1. **경로 하드코딩 금지.** `'C:\Windows\Fonts'` 대신 `os.environ.get('WINDIR', r'C:\Windows')` 기준으로 조립하고 `sys.platform == 'win32'` 일 때만 설정한다. 젯슨(Linux)에서 돌 가능성을 남긴다.
    2. **offscreen 을 쓰는 검증 스크립트 전부에 적용.** (`테스트_v1결함_재발검사` / `테스트_레이아웃_검증` / `테스트_실데이터_재생` / `테스트_평면도_경보흐름`)
       일부만 고치면 스위트 안에서 폰트 환경이 둘로 갈라진다. 중복이어도 각 파일에 넣는다 — `CLAUDE.md` §9, 검증 스크립트는 최소화 규칙의 예외다.
    3. **주석으로 이유를 남긴다.** `⚠ [8/02 실측]` 형식으로 위 근거 3과 수치를 남긴다.
    4. **`resolve_font()`(console_ui.py:111)에 방어 로그 추가.** `families()` 가 비었을 때 조용히 초기값으로 넘어가지 말고 경고 한 줄을 출력한다. **동작은 바꾸지 말고 출력만 추가.**
       이유: 실제 데모 PC 에서 폰트 캐시가 깨지거나 Noto Sans KR 이 빠지면 지금 구조로는 **아무 신호 없이** 글자가 밀린다. `CLAUDE.md` §9 — 실패를 조용히 삼키지 않는다.

  - 수정 후 `ui-verify` **전체**를 다시 돌려 레이아웃 0건 / 나머지 항목 변화 없음을 확인한다.

- **[8/02·2순위] vib 경보 시 인체 도식 초록** — 5/5 재현으로 결정적 결함 확정(§2). **원인 규명까지만 하고 멈춘다.**
  - **왜 멈추나**: `replay_jsonl.py` 는 `classify()` 를 돌리지 않고 jsonl 의 label 을 그대로 통보한다. 즉 테스트가 먹이는 **vib 패킷 시퀀스가 실제 젯슨 출력과 같은지 검증된 적이 없다.**
    - 원인이 **UI 렌더 순서** → 젯슨을 붙여도 똑같이 틀리게 그린다. 고칠 가치 있음.
    - 원인이 **재생 데이터의 vib 시퀀스가 `jetson_sender.py` 실제 출력과 다름** → 테스트 데이터 문제다. 8/04 젯슨 연결 후에 확정한다.
  - 확인 방법: `jetson_sender.py` 가 vibration_anomaly 에서 `ev.active` 를 어떻게 내는지와, 재생 데이터의 vib 패킷 시퀀스를 대조한다.
  - 어느 쪽인지 §2 에 보고하고 **승인받기 전에는 고치지 않는다.** 표시 계층만 대상이며 판정 경로는 건드리지 않는다.

- **[8/02·오늘 마지막] ① 검증 스크립트를 `cd` 없이 돌게 한다.**
  - **목적**: 복합 `cd A; python B` 명령은 Claude Code 가 매번 훅 실행 경고를 띄우고, 이 형태는 허용 목록으로 끌 수 없다. `cd` 자체를 없애는 게 유일한 해법이다.
  - 각 스크립트 상단(`QT_QPA_PLATFORM` 설정 근처)에 `os.chdir(os.path.dirname(os.path.abspath(__file__)))` 를 넣어 자기 위치에서 돌게 한다. `verify_port.py` 가 이미 "경로를 스스로 찾는다"고 되어 있으니 그 방식에 맞추는 것이다.
  - 대상: 테스트 4종 + `replay_jsonl.py` + `sim_jetson.py` 중 **cwd 에 실제로 의존하는 것만.** 어느 파일이 의존하는지 먼저 확인하고 필요한 데만 넣는다.
  - 확인: 프로젝트 루트에서 `cd` 없이 `python 01_현행코드\테스트_레이아웃_검증.py` 가 도는지 실제로 실행해 본다.
  - **문서도 같이 고친다.** 안 고치면 지시가 거기 남아 있어 Claude Code 가 계속 `cd` 를 붙인다.
    `.claude/skills/ui-verify/SKILL.md` · `.claude/skills/replay-regression/SKILL.md` · `CLAUDE.md` §5 의 실행 예시.
  - 마지막에 `ui-verify` 전체 재실행 — 결과가 이전과 **동일한지** 확인한다(항목 수·통과 수 그대로여야 한다).

- **[8/04(월)·최우선] 젯슨 통합시험.** 절차·실패 대응·확인 항목은 **`04_문서/젯슨_통합시험_런북_0804.md`** 에 있다. 세션 시작 시 그 파일부터 읽는다.
  - 오늘 목표는 **"흐르는가"** 뿐이다. 화면 다듬기·성능 튜닝은 하지 않는다.
  - 시작 전 방화벽 UDP 5005 인바운드 허용 → 핫스팟 연결 → **한 구간씩** 연다(노트북 단독 → 젯슨 단독 → 연결 → 전 과정).
  - 끊김이 **실제로 보일 때만** pyinstrument 로 측정한다. 안 보이면 손대지 않는다.
  - 결과는 반드시 **숫자로** §2 에 남긴다 (프레임레이트, 포인트 수, 유실률, LLM 응답 시간). 못 잰 건 `—`.

- **[보류 · 8/04 실측 후 결정] LLM prewarm 트리거 재설계.**
  - 문제: `prewarm()` 이 LIVE 진입 자동 트리거 하나에만 의존한다. 생성이 3유형 × 수십 초라 LIVE 직후 경보가 뜨면 캐시가 비어 있다.
  - 유력한 방향: **준비 단계(WARMUP/TRAINING)에서 시작.** 그 수십 초 동안 판정은 젯슨이 하므로 노트북 CPU 가 논다. SOP 생성은 `_gen(et, '')` 로 젯슨 상태와 무관하다.
  - **⚠ 지금 고치지 않는다.** jsonl 재생 때 LLM 응답에 약 1분이 걸렸는데 원인이 미확정이다(모델 로딩 / prewarm 미실행 / 생성 자체). **8/04 실측 숫자가 나온 뒤에 설계한다.** 추측 기반 수정 금지.
  - 임시 대책: 데모 전 `ollama run gemma2:2b "안녕" --keepalive 30m` 로 모델을 올려둔다.

- **공통**: 원인을 먼저 글로 설명하고, 승인받은 뒤에 고친다. 고친 뒤에는 `ui-verify` 전체를 다시 돌린다(한 개만 다시 돌려 통과했다고 보고하지 않는다).

- **[8/02·보류] UI freezing.** ⚠ **현재 확인된 문제가 아니다.** freezing 은 구버전 UI 에서 겪은 것이고, 현행 v2 는 아직 젯슨을 붙여 10 Hz 실데이터로 돌려본 적이 없다.
  - 정적 분석에서 나온 **잠재 위험**만 기록해 둔다. 지금 손대지 말 것.
    1. `console_ui.py:1408 DashboardPage.push()` — 패킷마다(10 Hz) 라벨 4개에 `setStyleSheet()`. Qt 스타일 캐스케이드 전체 재계산
    2. `self.plan.update()` 10 Hz → `FacilityPlan`(681~934) 250줄 `paintEvent` 전체 재드로우
    3. `Track3D` GLViewWidget — MX450 + 드라이버 452 OpenGL 스톨 가능성
  - **아닌 것**: LLM 추론 속도. `SopEngine`은 별도 스레드, `RadarLink`는 `QThread`. 여기를 파지 말 것.
  - 젯슨 연결 실측에서 실제로 끊김이 보이면 그때 pyinstrument 로 측정해 확정한다.

---

## 2. Claude Code → Cowork  (결과 · 막힌 것 · 판단 요청)

> Claude Code만 고친다. Cowork는 읽고 문서·보고서에 반영한다.

- **[8/02, 종료] 레이아웃 잘림 9건 / vib 도식 색 / set_mode() 즉시 재렌더 — 전부 해결·검증 완료.** 원인·근거는 git log 참조(이번 커밋에 포함). ui-verify 각 수정 직후 전부 통과시켜 뒀다.
- **[8/03] `SopEngine.prewarm()`의 `_w()` 침묵 수정.** LLM 사전 생성 실패 시 조용히 `return`만 하던 것을 `self.status.emit(...)`으로 알리고 `return`하도록 고침(`resolve_font()`와 같은 종류, `CLAUDE.md` §9). 동작(중단)은 그대로. **ui-verify 전체 재실행**: pyflakes 0 / v1 결함 재발 35항목 0건 / 레이아웃 4해상도 0건 / 실데이터 재생 64항목 0건 / 평면도 경보흐름 16항목 0건 — 전부 이전과 동일.
- **판단 요청 ①(유지, 미해결): 링크 다운 시 색은 맞아도 stale 베일이 최대 3초 안 뜨는 창이 있음.** `LINK_TIMEOUT`(3.0s, radar_common.py, 안 건드림) 전 구간엔 베일 없이 색만 정확 — 실측 확인됨(1.8s 경과 시점, 색 `#F59E0B` / 베일 미표시). §1에서 필요·불필요 판단 바람.
- **판단 요청 ②(상태 변경: 미해결 · 8/04 실측으로 확인) `on_packet`의 `on_alert`(2484행 부근)가 `_pump_state`(2524행)보다 먼저 계산됨.** 낙상/정지형 경보가 막 시작된 그 패킷 한 프레임(~100ms)은 `hide_shape`가 늦게 걸릴 수 있다.
  **지금 안 고치는 이유**: 한 프레임(~100ms) 타이밍이라 현재 검증 스위트로는 재현·검증이 안 된다. 검증할 수 없는 수정을 통합시험 직전에 넣지 않는다 — 8/04 젯슨 실측으로 실제로 보이는지 먼저 확인한 뒤 판단.
- **README 정정 필요(유지)**: 8/1 "실데이터 재생 57항목 0건"은 잘못된 기록.
- **다음 세션**: §1 젯슨 통합시험 런북대로 진행. 판단 요청 ①·②는 8/04 실측 결과를 보고 §1에서 지시.

### 여기에 쓸 것
- 검증 결과 **숫자** (예: "v1 결함 재발 35항목 0건") → Cowork가 README·보고서에 그대로 쓴다
- **막힌 것**: 환경 문제, 물리 한계, 판단이 필요한 갈림길
- **§1의 지시와 다르게 한 것과 그 이유** — 이게 가장 중요하다. 말없이 다르게 하면 Cowork가 틀린 문서를 쓴다
- 다음 세션에서 이어서 할 것

### 여기에 쓰지 말 것
- 파일별 변경 내역 (→ `git diff`)
- 코드 조각 (→ 파일 자체가 정본)
- 세션에서 한 일의 나열
