# HANDOFF — Cowork ↔ Claude Code 인계 노트

> **이건 로그가 아니라 화이트보드다.** 항상 "지금"만 적는다. 끝난 항목은 지우고, 남길 가치가 있으면 `README.md`로 옮긴다.
> **변경 내역은 여기 쓰지 않는다.** 무엇이 바뀌었는지의 정본은 `git log` / `git diff`다. 여기엔 git이 담을 수 없는 것만 쓴다 — **의도 · 판단 · 막힌 지점.**
> 각 역할은 **자기 섹션만** 고친다. 남의 섹션은 읽기만 한다.
> 갱신 시점: **세션을 끝낼 때.** 작업 도중에 계속 쓰지 않는다 (동시 편집 충돌).
> 분량: 각 섹션 **10줄 이내.** 넘치면 오래된 것부터 지운다.

---

## 0. 현재 상태 (양쪽 다 읽는다 · 마지막에 작업한 쪽이 갱신)

- **최종 갱신**: 2026-08-02 · Claude Code
- **git**: `main` / 초기 커밋 `df28e8e` (129파일)
- **지금 무슨 단계인가**: vib 인체 도식 초록 NG를 5회 연속 재현 → **결정적 결함으로 확정** (경합 아님). 아직 미해결. 원인 분석·수정은 다음 세션.
- **다음 최우선**: ① vib 인체 도식 색 결함 수정(원인 분석 먼저) → ② 레이아웃 잘림 9건(폰트 확인 먼저). 아래 §1·§2 참조.

---

## 1. Cowork → Claude Code  (설계 결정 · 지시)

> Cowork만 고친다. Claude Code는 읽고, 작업 시작 전에 여기 내용과 어긋나는 게 있으면 먼저 묻는다.

- **[8/02·1순위] vib 경보 시 인체 도식이 초록으로 그려진다** (`테스트_실데이터_재생.py` 57항목 중 1건 NG).
  - **왜 1순위인가**: `CLAUDE.md` §4 — 색이 등급을 의미한다. 진동 이상 경보 중에 화면이 초록(정상)으로 보이면 관제 화면이 거짓말을 하는 것이다. 잘림은 읽기 불편한 것이고 이건 잘못 읽히는 것이다.
  - 1차 분석: `cur_sev()`(console_ui.py:2422)와 `sev_colors()`(radar_core.py:862)는 warning 을 내는데, 실제로 그려진 펜 색(radar_core.py:899 `self.cap.setPen(...)`)만 GREEN 이다. fall·still 은 같은 경로인데 통과 → **경합(race) 의심**.
  - **먼저 할 것: flaky 여부 확인.** `테스트_실데이터_재생.py` 를 **연속 5회** 돌려 매번 같은 항목이 NG 인지 본다.
    - 매번 NG → 결정적 결함. 이벤트 활성/해제 시점과 렌더 순서를 추적해 고친다.
    - 들쭉날쭉 → 경합. 순서 보장(상태기계 → 렌더)이 vib 경로에서 깨지는 지점을 찾는다. 이 경우 **8/1 의 "57항목 0건"도 우연히 통과한 값**이라는 뜻이므로 §2에 반드시 보고할 것 (README 수치를 정정해야 한다).
  - 표시 계층만 고친다. **판정 경로(`jetson_sender.py`, 판정 상수)는 건드리지 않는다.**

- **[8/02·2순위] `테스트_레이아웃_검증.py` 잘림 9건** (1440×900 ~ 1200×800). "시점 초기화", 상단 타임스탬프, 구역 캡션, 경보 화면의 "실시간 감시"·"낙상 감지 · A 구역" 등.
  - 8/1 에 이 테스트는 4해상도 **0건**으로 통과했고 그 뒤 `console_ui.py`는 **한 줄도 바뀌지 않았다**(초기 커밋이 그때 파일 그대로).
    → 코드 회귀가 아니라 **환경 변화**를 먼저 의심할 것. Python 3.14.4 → `resolve_font()`(console_ui.py:111)가 고른 폰트 → offscreen 플랫폼 기본 폰트 metrics 순.
    실제로 어떤 폰트가 잡혔는지 **출력해서 확인**한 뒤에 판단한다. 추측으로 고치지 않는다.
  - **검사 기준을 무르게 고쳐 통과시키는 것은 금지.** 잘림이 진짜면 관제 화면에서 글자가 잘려 보인다는 뜻이고 그건 결함이다. 기준이 틀렸다고 판단되면 근거를 제시하고 확인받은 뒤에 고친다.
  - 글자를 줄여 해결하는 것은 최후 수단. `CLAUDE.md` §7 — 화면에 설명을 늘리지 않는 것과, 필요한 정보를 지우는 것은 다르다.

- **공통**: 원인을 먼저 글로 설명하고, 승인받은 뒤에 고친다. 고친 뒤에는 `ui-verify` 전체를 다시 돌린다(한 개만 다시 돌려 통과했다고 보고하지 않는다).

- **[8/02·보류] UI freezing.** ⚠ **현재 확인된 문제가 아니다.** freezing 은 구버전 UI 에서 겪은 것이고, 현행 v2 는 아직 젯슨을 붙여 10 Hz 실데이터로 돌려본 적이 없다.
  - 정적 분석에서 나온 **잠재 위험**만 기록해 둔다. 지금 손대지 말 것.
    1. `console_ui.py:1408 DashboardPage.push()` — 패킷마다(10 Hz) 라벨 4개에 `setStyleSheet()`. Qt 스타일 캐스케이드 전체 재계산
    2. `self.plan.update()` 10 Hz → `FacilityPlan`(681~934) 250줄 `paintEvent` 전체 재드로우
    3. `Track3D` GLViewWidget — MX450 + 드라이버 452 OpenGL 스톨 가능성
  - **아닌 것**: LLM 추론 속도. `SopEngine`은 별도 스레드, `RadarLink`는 `QThread`. 여기를 파지 말 것.
  - 젯슨 연결 실측에서 실제로 끊김이 보이면 그때 pyinstrument 로 측정해 확정한다.

---

## 2. Claude Code → Cowork  (결과 · 막힌 것 · 판단 요청)

> Claude Code만 고친다. Cowork는 읽고 문서·보고서에 반영한다.

- **[8/02] vib 경보 시 인체 도식 초록** — `테스트_실데이터_재생.py`를 연속 5회 실행, **매번 동일하게 NG 1건** (통과 56 / 실패 1, 5/5 재현). 경합이 아니라 **결정적 결함**. 원인 분석·수정은 아직 안 함(다음 세션 작업).
- **README 정정 필요**: 위 결과로, 8/1 "실데이터 재생 57항목 0건"은 **잘못된 기록**이다. 그 시점에도 이 결함은 존재했을 것으로 보이며 우연히 통과 처리된 값이다. Cowork가 README 수치를 정정할 것.
- **관찰(2순위 단서)**: 테스트 실행 중 stderr에 `QFontDatabase: Cannot find font directory ...Qt5/lib/fonts` 경고가 반복 출력됨. §1 2순위(레이아웃 잘림 9건)의 `resolve_font()` 폰트 선택 문제와 연결될 가능성이 높다. 다음 세션에서 `resolve_font()`가 실제로 어떤 폰트를 잡았는지 먼저 출력해서 확인할 것 — 추측으로 고치지 않는다.
- **다음 세션**: vib 색 결함 원인 분석 후 승인받고 수정 (표시 계층만, `jetson_sender.py`·판정 상수는 건드리지 않는다).

### 여기에 쓸 것
- 검증 결과 **숫자** (예: "v1 결함 재발 35항목 0건") → Cowork가 README·보고서에 그대로 쓴다
- **막힌 것**: 환경 문제, 물리 한계, 판단이 필요한 갈림길
- **§1의 지시와 다르게 한 것과 그 이유** — 이게 가장 중요하다. 말없이 다르게 하면 Cowork가 틀린 문서를 쓴다
- 다음 세션에서 이어서 할 것

### 여기에 쓰지 말 것
- 파일별 변경 내역 (→ `git diff`)
- 코드 조각 (→ 파일 자체가 정본)
- 세션에서 한 일의 나열
