# AGENTS.md — Radar-Guard 프로젝트 규칙

> 2026 한이음 드림업 공모전 · 비접촉 레이더 기반 고보안 현장 작업자 안전 관리 에이전트
> 담당: 홍유빈 — 시스템 통합 / UI / RAG
> 이 파일은 Codex와 Cowork 양쪽이 세션마다 읽는다. **추측하지 말고 여기 적힌 것을 따른다.**

## ⛔ 판정 코드 동결 — 2026-08-24 ~ 해제 시까지

> **낙상 · 정지형(무동작) · PINCH 모션 · 판정값/임계값 — 판정에 관여하는 코드는 아예 건드리지 않는다.**
> UI 작업을 포함한 **모든** 작업에 적용된다. 고쳐야 할 이유가 생기면 **먼저 홍유빈에게 허락을 받는다.**
> 8/24 시연에서 낙상·협착·감전 경로가 전부 정상 동작하는 것을 확인했다. 그 상태를 기준선으로 고정한다.

### 동결 — 파일 전체 (열지도 말 것)

| 파일 | SHA-256 (8/24 기준선) |
|---|---|
| `01_현행코드/jetson_sender.py` | `18d2ede32dda6148c44d61a4e169cf271069d020351444c5c05169478aa76a2a` |
| `01_현행코드/verify_jetson_safe.py` | `2c931a733651ff982d4353fb04da66eb4e90c48cba191d806ec7ecbd359e4a90` |
| `01_현행코드/train_fall_safety.py` | `30023bdfbb315d15ba8762026473de236df745b1e9a52a5d34b4d5529409adc5` |
| `01_현행코드/*.joblib` 모델 3개 | 재학습·교체·경로 변경 금지 |

### 동결 — `radar_common.py` 안의 판정 항목

`SCHEMA_VERSION` · `DATA_PORT` · `CTRL_PORT` · `CMD_*` · `CEILING_H` · `FRAME_INNER_HALF` ·
`ENTRY_BAND` · `OCCUPANCY_CORE_HALF` · `PH_*` · `RADAR_ZONE` · `ZONE_IDS` · `EVENT_ZONE` ·
`SEV_RANK` · **`EVENT_SEV`** · **`AUTO_TRIP_EVENTS`** · **`CURR_LIMIT`** · **`VOLT_MIN`** ·
`POWER_CONFIRM` · `LEAK_LIMIT` · `LEAK_CONFIRM` · `VIB_DS_THRESH`
그리고 **모든 딕셔너리의 키**(이벤트 타입 문자열).

### 수정해도 되는 것 — 오늘 저녁 UI 작업 범위

- `01_현행코드/console_ui.py` — 표시 전용
- `01_현행코드/radar_core.py` — 표시 전용. 단 **판정을 UI에서 다시 계산해서 그리지 않는다**
- `radar_common.py` 는 **표시 문구·색·폰트만**: `PHASE_KO` `PHASE_ACTION` `EVENT_KO` `ZONE_KO`
  `SEV_KO` `GATE_META` `REJECT_KO` `EVIDENCE_KO` `BREAKER_SCOPE` · 팔레트(`BG`~`FS_*`)
  → **값(한글 문구·색상)만 바꾼다. 키는 절대 못 바꾼다.**

### 작업 후 반드시 실행하고 결과를 붙일 것

```bash
git --no-optional-locks diff --stat 01_현행코드/
sha256sum 01_현행코드/jetson_sender.py 01_현행코드/verify_jetson_safe.py
grep -nE "^(CURR_LIMIT|VOLT_MIN|POWER_CONFIRM|LEAK_LIMIT|LEAK_CONFIRM|VIB_DS_THRESH) *=" 01_현행코드/radar_common.py
grep -n "AUTO_TRIP_EVENTS" -A4 01_현행코드/radar_common.py
python 01_현행코드/verify_jetson_safe.py
```

기대값 — `jetson_sender.py` 변경 **0줄** · 위 SHA-256 **불변** ·
`CURR_LIMIT=0.10` `VOLT_MIN=7.50` `POWER_CONFIRM=2` `LEAK_LIMIT=0.008` `LEAK_CONFIRM=2` `VIB_DS_THRESH=0.20` ·
`AUTO_TRIP_EVENTS` 원소 6개이며 `fall_detected` **없음** · `verify_jetson_safe.py` 종료코드 0.

**하나라도 어긋나면 커밋하지 말고 되돌린다.**

> 상세·근거: `04_문서/설계/판정코드_동결_0824.md`
> 협착 PINCH 판정도 동결 대상이다 — `PINCH_SHADOW_WIN=20` `PINCH_DS_MEAN_MIN=0.33` `PINCH_DS_MAX_MAX=0.59` `PINCH_HEIGHT_MEAN_MAX=0.83`.
> **8/24 시연자 A 고정 규칙으로 채택**했다(A PINCH 8/10·A 정상 30건 오탐 0). 일반화 규칙이 아니다(B 4/10 · C 1/10).
---

---

## 0. 가장 중요한 3가지

1. **이건 안전 시스템이다.** 낙상·감전을 놓치면 사람이 다친다. "아마 될 것"으로 넘어가는 변경은 없다. 모든 수정은 §5의 검증을 통과해야 한다.
2. **판정 경로와 표시 경로는 절대 섞지 않는다.** 젯슨이 판정하고, 노트북은 그것을 그린다. UI 계산 결과를 판정에 되먹이면 안 된다.
3. **모르면 묻는다.** 애매한 지점에서 임의로 정하지 말고 멈추고 물어본다. 제안을 받아도 타당성을 먼저 검토하고, 걸리는 게 있으면 그대로 진행하지 말고 대안을 제시한다.

---

## 1. 아키텍처 (2026-07-28 최종 확정)

```
[IWR6843 레이더] --UART--> [Jetson Orin Nano]  --UDP :5005--> [노트북]
                            판정 전용                            관제 UI + LLM + SOP DB
                            classify() / LSTM-AE              <--UDP :5006-- 제어 명령
                            차단기 제어
```

| | 젯슨 (엣지) | 노트북 (관제) |
|---|---|---|
| 역할 | 낙상·감전 **판정**, 차단기 차단 | 화면, LLM 브리핑, SOP RAG |
| 파일 | `jetson_sender.py` | `console_ui.py` + `radar_core.py` |
| 환경 | JetPack 7.2 (R39 / CUDA 13.2), sm_87 | Windows, MX450 (VRAM 2GB) |
| 접속 | 시리얼 **COM13** | — |
| LLM | 없음 | Ollama, CPU 추론 **13.45 tok/s** 실측 |

**젯슨이 죽어도 판정은 젯슨에서 끝난다.** 노트북 UDP가 끊겨도 차단은 동작해야 한다. 노트북의 제어 명령(`CTRL_PORT`)은 젯슨 로컬 제어의 **대체가 아니라 보조**다.

---

## 2. 폴더 구조

| 경로 | 내용 | 누가 만지나 |
|---|---|---|
| `01_현행코드/` | **현행 코드. 여기만 실행된다.** | Codex |
| `02_레이더_원본코드/` | 팀 원본. `radar_live_full.py`가 판정의 정본 | 읽기만 |
| `03_데이터/` | 실측 jsonl (70MB) | 읽기만 |
| `04_문서/` | 설계·진행보고 | Cowork |
| `05_발표자료/`, `06_부스_하우징/`, `07_중간보고서_7.14/` | 산출물 | Cowork |
| `08_주차별_진행도/` | 학습 기록 (week1~5) | 읽기만 |
| `10_팀협업/` | 팀원 코드·인터페이스 명세 | 협의 후 |
| `scripts/` | 저장소 운영·문서·프로세스 검증 자동화 도구. 제품 런타임에 포함되는 애플리케이션 코드는 두지 않는다. | 양쪽 |
| `_구버전보관/`, `_삭제후보/` | **참조 전용. 절대 여기 코드를 고치지 않는다.** | 금지 |
| `README.md` | 개발 일지 (2,653줄) | §6 형식 준수 |

**`_구버전보관/`에도 `console_ui.py`가 있다.** 파일명만 보고 열지 말고 항상 경로를 확인한다.

---

## 3. 현행 코드 (`01_현행코드/`)

### 실행 파일
| 파일 | 줄수 | 역할 |
|---|---:|---|
| `console_ui.py` | 2,953 | **화면 전부 + 진입점.** `build_app()` → `ConsoleV2` |
| `radar_core.py` | 1,953 | `RadarLink`(수신 스레드) · `PoseEstimator` · `SopEngine` · `Track3D` · `PreparePage` · 팝업 6종 |
| `radar_common.py` | 375 | **젯슨/노트북 공용 상수. 양쪽이 같은 파일을 본다.** |
| `facility.py` | 123 | 시설 평면도 좌표 |
| `jetson_sender.py` | 1,689 | 젯슨 측 — 판정 + UDP 송신 |

`console_ui.py`는 `radar_core.py`를 **import** 한다. 복사가 아니라 같은 코드 객체다. 이 관계를 깨지 않는다.

### 검증 · 도구
| 파일 | 용도 |
|---|---|
| `테스트_v1결함_재발검사.py` | v1에서 고친 결함 재발 검사 (기준: 0건) |
| `테스트_레이아웃_검증.py` | 4해상도 잘림/겹침 (기준: 0건) |
| `테스트_실데이터_재생.py` | 실측 jsonl → UDP → UI 전 경로 |
| `테스트_평면도_경보흐름.py` | 사전경보 → latch → 자동전환 → 종료 |
| `verify_port.py` | `classify()` 이식 무손실 회귀 (numpy만 필요) |
| `verify_jetson_safe.py` | 노트북용 수정이 젯슨을 안 깬다는 증명 |
| `sop_doctor.py` | 벡터DB 실제 내용 진단 |
| `replay_jsonl.py` | 실측 재생기 (눈으로 볼 때) |
| `sim_jetson.py` | 난수 모의 송신기 (프로토콜 검증용, numpy만) |

---

## 4. 절대 하지 말 것

- **레이더 `.cfg` 수정 금지.** 포인트 수 부족은 각분해능 28° 병목이 원인이고 cfg로 해결되지 않는다. 누적으로만 다룬다.
- **판정 상수를 UI 편의로 바꾸지 않는다.** `radar_common.py`의 임계값(`VIB_DS_THRESH`, `CURR_LIMIT`, `VOLT_MIN` 등)을 바꾸면 `verify_port.py`가 깨진다. 바꿔야 하면 근거와 재학습 계획을 먼저 낸다.
- **`radar_common.py`는 젯슨과 노트북이 공유한다.** 한쪽 편의로 고치면 다른 쪽이 죽는다.
- **`_구버전보관/`, `_삭제후보/` 안의 파일을 수정하지 않는다.**
- **`03_데이터/`의 jsonl을 덮어쓰지 않는다.** 재수집 비용이 크다.
- **RED(`#EF4444`)는 위험 경보 외에 절대 쓰지 않는다.** 색이 등급을 의미한다.
- **화면에 설명 문구를 늘리지 않는다.** §7 참조.
- **없는 수치를 그럴듯하게 채우지 않는다.** 데이터가 없으면 `—`로 둔다. 관제 화면의 거짓 수치는 결함이다.

---

## 5. 수정 후 반드시 실행 (환경: **내 PC PowerShell**)

`console_ui.py` / `radar_core.py` / `radar_common.py`를 건드렸으면(`cd` 불필요 — 검증 스크립트는 실행 위치와 무관하게 자기 위치 기준으로 돈다):

```powershell
python -m pyflakes 01_현행코드\console_ui.py 01_현행코드\radar_core.py 01_현행코드\radar_common.py 01_현행코드\facility.py
python 01_현행코드\테스트_v1결함_재발검사.py
python 01_현행코드\테스트_레이아웃_검증.py
python 01_현행코드\테스트_실데이터_재생.py
python 01_현행코드\테스트_평면도_경보흐름.py
```

`jetson_sender.py` / `radar_common.py`를 건드렸으면 추가로:

```powershell
python 01_현행코드\verify_port.py
python 01_현행코드\verify_jetson_safe.py
```

**통과 기준은 전부 0건 / 종료코드 0이다.** 하나라도 NG면 완료라고 보고하지 않는다.
검증 스크립트는 `QT_QPA_PLATFORM=offscreen`으로 돌아 창이 뜨지 않는다.

---

## 6. README 작성 형식

README는 개발 일지이자 **보고서 원본**이다. 그래서 형식이 정해져 있다.

- **문제 → 해결 순서.** "무엇을 만들었다"가 아니라 "무엇이 문제였고 왜 이렇게 풀었다".
- 날짜 헤더로 세션을 구분한다 (`# 🟢 8/01 — 제목`).
- 항목마다 **문제 / 해결**을 굵게 구분하고, 근거가 되는 실측 수치를 넣는다.
- 검증 결과는 숫자로 쓴다 ("v1 결함 재발 35항목 0건").
- 최신 세션이 위로 간다.

---

## 7. UI 설계 원칙 (2026-07-29 확정)

1. **3D 포인트 클라우드가 메인이다.** 다른 요소가 이걸 밀어내지 않는다.
2. **평소 화면과 경보 화면을 분리한다.** 경보 시 볼 것만 남는다.
3. **화면에 설명을 쓰지 않는다.** 관제 근무자는 읽지 않는다. 색과 위치로 말한다.
4. **버튼은 뒤로 뺀다.** 상시 노출은 상태 표시, 조작은 한 단계 뒤.
5. **위험 조작은 확인을 받는다.** 전원 재투입 등은 `confirm()` 필수.

배제 확정: FFT 스펙트럼, SYSTEM HEALTH 패널. (개발용 정보이지 관제 정보가 아니다)

LLM은 4레이어로 쓴다 — L1 브리핑 / L2 XAI 근거 / L3 RAG / L4 질의. **LLM은 안전 조치의 필수 경로가 아니다.** 실패해도 하드코딩 즉시조치와 검색 원문은 이미 화면에 있어야 한다.

---

## 8. 알려진 물리적 한계 (코드로 고치려 들지 말 것)

- **각분해능 28°** → 포인트 수가 근본적으로 적다. 8점/프레임을 10프레임 누적해 ~80점으로 쓴다.
- **정지한 사람은 FMCW 레이더가 놓친다.** 도플러가 0이라 사라진다. 그 자리를 다중경로·바닥반사 링이 채운다.
  → 낙상 경보 중인데 점군이 "서 있음"이면 **사람이 아니라 추적을 놓친 것**이다. 형상을 지우고 이유를 표시한다.
- **젯슨에 RTC 배터리가 없다.** 부팅마다 시계가 틀어진다. **모든 경과시간은 노트북 수신 시각 기준**으로 계산한다.
- **노트북 GPU(MX450)로 LLM 가속 불가.** VRAM 2GB + 드라이버 452. 7~8B 모델 상향은 폐기됐다. 병목은 속도가 아니라 한국어 품질이다.
- **임베딩은 `bge-m3`로 통일.** 적재와 검색이 다른 모델이면 차원이 안 맞아 조용히 0건이 나온다 (`sop_doctor.py` 참조).

---

## 9. 코딩 규칙

- **주석은 한국어.** 왜 이렇게 했는지를 쓴다. 특히 실측으로 알아낸 것은 날짜와 수치를 남긴다 (`⚠ [8/01 실측] …`).
- **파일 상단 docstring에 실행 방법을 적는다.** 어느 환경(PowerShell / 젯슨 / VS Code)인지 명시한다.
- **예외를 조용히 삼키지 않는다.** 실패는 화면이나 로그에 표시한다.
- **센티넬 값을 UI로 흘리지 않는다.** (`1e9` 대신 `None` → "연결 전")
- 새 기능은 **필요한 최소만** 쓴다. 추측 기반 추상화·미래 대비 스캐폴딩 금지. 단, **검증 스크립트와 안전 방어 코드는 이 규칙의 예외**다 — 중복이라도 남긴다.
- **`cd`를 붙이지 않는다.** git 명령은 세션이 이미 저장소 루트다. `01_현행코드/`의 검증 스크립트(테스트 4종 · `replay_jsonl.py` · `sim_jetson.py` · `verify_port.py` 등)도 전부 자기 위치 기준으로 돌게 돼 있어 `cd` 없이 `python 01_현행코드\파일.py` 형태로 바로 실행한다. `cd A; python B` 형태는 매번 훅 실행 경고를 띄운다.

---

## 10. Codex ↔ Cowork 역할 분리

둘은 같은 폴더를 보지만 **기억을 공유하지 않는다.** 같은 파일을 동시에 고치면 서로 덮어쓴다.

| | Codex (PowerShell) | Cowork (데스크톱 앱) |
|---|---|---|
| 역할 | **개발자** — 코드 작성·실행·디버깅 | **기획·설계·문서** — 무엇을 왜 만들지 정하고 기록한다 |
| 담당 | `01_현행코드/` | `04_문서/`, `05_발표자료/`, `06_부스_하우징/`, `README.md`, Notion |
| 이유 | 실제로 `python`을 돌리고 COM13·젯슨에 붙을 수 있다 | 조사·설계·발표자료에 강하고 파일 공유가 편하다 |

**한쪽이 작업 중인 파일은 다른 쪽에서 열지 않는다.** 코드 세션이 끝나면 `git commit` 후 문서 작업으로 넘어간다.

### 인계 노트

@HANDOFF.md

기억이 안 이어지므로 위 인계 노트가 **유일한 연락 수단**이다.

> **[8/03 구조 변경]** 소통과 실행을 분리했다. `HANDOFF.md` 는 **실행 지시서**이고, 소통은 `04_문서/AI_BRIDGE/` 다. 이전의 §0/§1/§2 3섹션 구조는 없어졌다 — 그 구조에서 §1 이 규칙(10줄)의 7.7배인 77줄까지 불어나 완료된 지시가 '할 일'로 읽히는 문제가 있었다.

1. **세션을 시작할 때 `HANDOFF.md` 를 읽는다.** 위 import 로 자동 로드된다. **`AI_BRIDGE/` 는 자동으로 읽지 않는다** — 명시적 sync 나 session-end triage 때만 연다.
2. **`HANDOFF.md` 는 `session-end` 에서만 전체 재작성한다.** 작업 도중에 고치지 않고, 기존 내용에 덧붙이지 않는다. 섹션은 6개(State / Current objective / Verified baseline / Next actions / Blockers / Acceptance)뿐이다.
3. **막힌 것·판단 요청·불일치는 `04_문서/AI_BRIDGE/OUTBOX.md` 에 쓴다.** HANDOFF 의 Blockers 에 직접 쓰지 않는다. Blockers 에는 아직 답이 없는 **미지수**만 둔다.
4. **완료 이력을 HANDOFF 에 남기지 않는다.** 정본은 `git log` / `git diff` 다. 손으로 적으면 반드시 어긋난다. 완료된 항목은 즉시 지운다.
5. **분량은 전체 40줄, Next actions 최대 3, Blockers 최대 3.** 커밋 전에 `python scripts\validate_handoff.py` 와 `validate_ai_bridge.py` 가 종료코드 0 이어야 한다.
6. **지시와 다르게 했으면 반드시 `OUTBOX.md` 에 이유를 적는다.** 말없이 다르게 하면 반대쪽이 틀린 문서를 쓴다.
7. **Cowork 가 README 를 정리할 때, 그 세션에서 나온 우려·설계 변경·반대쪽이 알아야 할 사항은 묻지 말고 `REVIEW.md` 에 함께 올린다.** README 는 일지라 결론만 남고, 아직 결론이 안 난 것은 검토 왕복이 필요하다. 특히 **기존 설계를 바꾸는 제안**과 **반대쪽 작업을 막거나 무효화하는 사항**(편집 중 파일, 실측 전제, 검증이 못 잡는 변경)은 빠뜨리지 않는다.
8. **분석 문서에 행 번호를 쓰지 않는다.** 조건식·함수명·상수명으로 기술한다. ⚠ [8/10] 편집 중인 파일을 읽고 행 번호로 진단을 내 이미 해결된 항목 2건을 미해결로 보고했다. 행 번호는 편집 한 번에 무의미해지고, 읽는 쪽이 무효인지 판단할 수 없다.

정보마다 정본이 하나다 — 실행 지시는 `HANDOFF.md`, 소통은 `AI_BRIDGE/`(GPT 검토는 `REVIEW.md`), 개선 이슈는 `04_문서/BACKLOG.md`, 설계 근거는 `04_문서/`, 완료 이력은 `git log`.

### 규칙 문서 수정권

**`AGENTS.md` · `AGENTS.md` · `.agents/skills/` · `.agents/skills/` 를 고칠 수 있는 것은 Cowork 뿐이다.**
Codex 와 Codex 는 직접 고치지 않고 `04_문서/AI_BRIDGE/OUTBOX.md` 에 제안한다.

- 정본은 **`AGENTS.md` 와 `.agents/skills/`** 다. `AGENTS.md` 와 `.agents/skills/` 는 `scripts\sync_agent_docs.py` 의 **생성물**이다.
- 생성물을 직접 고치면 정본과 어긋나 **커밋이 막힌다**(pre-commit 이 `--check` 를 돌린다).
- ⚠ [8/04] 실제로 갈라진 적이 있다 — `AGENTS.md` 의 인계 규칙이 이미 없어진 §0/§1/§2 구조를 지시했고 `session-end` 스킬은 74줄 차이가 났다. **두 도구가 다른 규칙으로 움직이면 반대쪽이 틀린 문서를 쓴다.**

---

## 11. 일정

- **2026-08-17** 멘토링
- **10~11월** 최종 발표

심사는 **영상 시연 우선**. 동상 이상 확정 시에만 현장 시연. → 외관보다 **레이더 성능**이 최우선이다.

## Imported Claude Cowork project instructions

정보 탐색시 확실한 정보인지 확인한 후 알려줄것
